<!DOCTYPE html>
<html>

<head>
  <meta charset="utf-8">
  <title>Passport Scrapbook App</title>
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
</head>

<body>

  <div class="container">
    <div class="page-header">
      <h1>Passport Scrapbook App</h1>
      {{#if user}}
      <a href="/logout">Log Out</a>
      {{/if}}
    </div>

    {{#if messages.error}}
    <div class="alert alert-danger">
      {{messages.error}}
    </div>
    {{/if}}
    <div>
      {{{body}}}
    </div>
  </div>
</body>

</html>
